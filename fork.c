#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    // int f1 = fork();
    // int f2 = fork();
    srand(time(NULL));
    int slec = 2+(rand()%4);
    // printf("r: %d\tseconds to sleep: %d\n",r,slec);
    // if (f1) {

    // }
    // else if (f2) {

    // }
    // else {

    // }

    return 0;
}